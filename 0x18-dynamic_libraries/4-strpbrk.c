#include "main.h"
/**
 * _strpbrk - returns the number of bits in the initial segment
 * @s: the memory to compare to
 * @accept: the character to check
 *
 * Return: a pointer with the count
 */

char *_strpbrk(char *s, char *accept)
{
}

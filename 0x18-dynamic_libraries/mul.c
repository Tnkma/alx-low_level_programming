#include "prototypes.h"

/**
 * mul - add up the parameters
 * @a: the first arguments
 * @b: the second arguments
 *
 * Return: the successful operation
 */

int mul(int a, int b)
{
	return (a * b);
}

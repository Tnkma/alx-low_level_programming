#include <stdio.h>

/**
 * main - Entry point
 *
 *
 * Return:Always 0 (success)
 */

int main(void)
{
	int n;
	/**integer n is going to hold the alphabets while we loop through**/
	for (n = 'a'; n <= 'z'; n++)

		putchar(n);
	putchar('\n');

	return (0);
}

#include "main.h"

/**
 * mul - mutiplies two integers
 * @a: first value
 * @b: second value
 *
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);

	return (0);
}

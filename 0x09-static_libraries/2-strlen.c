#include "main.h"

/**
 * _strlen - the function to count the string
 * @s: the string to be counted
 *
 * Return: Always void
 */

int _strlen(char *s)
{
	/* A variable to count the string */
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{

	}
	return (c);
}
